<h1>Settings</h1>

<div id="settings-form" class="center-box">
  <%= form_for @user do |f| %>
    <div class="checkbox">
      <label>
        <%= f.check_box :artist_notification %> Get new release email notifications for artists you follow
      </label>
    </div>
    <div class="checkbox">
      <label>
        <%= f.check_box :wishlist_notification %> Get email notifications when records in wish list release
      </label>
    </div>
    <div class="form-button">
      <%= f.submit "Update", class: "btn btn-default" %>
    </div>
  <% end %>
</div>
